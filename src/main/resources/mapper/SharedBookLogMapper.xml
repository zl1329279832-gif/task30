<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.jackbin.SimpleRecord.mapper.SharedBookLogMapper">

    <select id="queryByBookId" resultType="cn.jackbin.SimpleRecord.entity.SharedBookLogDO">
        SELECT t1.*
        FROM tb_shared_book_log t1
        WHERE t1.record_book_id = #{recordBookId}
          AND t1.delete_time IS NULL
          <if test="operType != null and operType != ''">
              AND t1.oper_type = #{operType}
          </if>
        ORDER BY t1.create_time DESC
    </select>

</mapper>
