<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.jackbin.SimpleRecord.mapper.ClosingAdjustmentMapper">

    <select id="queryNetBudgetImpact" resultType="java.math.BigDecimal">
        SELECT IFNULL(SUM(budget_impact), 0)
        FROM tb_closing_adjustment
        WHERE book_id = #{bookId}
          AND year_month = #{yearMonth}
          AND delete_time IS NULL
    </select>

</mapper>
